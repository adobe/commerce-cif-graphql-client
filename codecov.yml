coverage:
  status:
    project:
      default:
        # basic
        target: 80%
        threshold: null
        base: pr 
        # advanced
        branches: null
        if_no_uploads: error
        if_not_found: success
        if_ci_failed: error
        only_pulls: false
        flags: null
        paths: null